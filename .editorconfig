root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

# Maven Wrapper supplies the Windows batch launcher with CRLF line endings.
[*.cmd]
end_of_line = crlf
