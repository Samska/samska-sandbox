# Project Handoff

Compact, current-state handoff notes. Each entry records only confirmed state changes, decisions, evidence, pending items, and the next step.

## SS-030 — Formalize Issue and Project Handoff Protocol (#47)

- State changes: `docs/AI-GOVERNANCE.md` defines the canonical Issue Handoff (11 ordered fields with the controlled `Planned`/`Awaiting approval`/`In Build`/`Blocked`/`Paused`/`Completed` status), Pull Request Handoff (12 fields), and `PROJECT_HANDOFF.md` snapshot rules. `.github/ISSUE_TEMPLATE/work-item.yml` renders the Issue Handoff through one Issue Form, and `.github/ISSUE_TEMPLATE/config.yml` disables blank issues; neither sets labels, assignees, or Project metadata. `.github/pull_request_template.md` provides the pull request handoff headings, and the existing Repository validation job fails when a required heading is missing, with `edited` added to the `pull_request` trigger so body corrections re-run it. `AGENTS.md` adds the session resume procedure, `CONTRIBUTING.md` documents contributor handoff expectations, `docs/GITHUB.md` records the Issue Forms control, and `docs/TESTING.md` describes the validation. Required status checks remain deferred.
- Decisions: one canonical protocol with pointers from AGENTS/CONTRIBUTING; one generic Issue Form; the Issue Form is the enforced UI path while API- or agent-created issues must reproduce the canonical fields manually; no automatic labels, assignees, or Project metadata; no ADR; no product code or dependencies.
- Agent Verification: Issue Form and workflow YAML inspected (canonical field order, controlled dropdown, required fields, no automatic metadata, blank issues disabled, three jobs and `contents: read` unchanged). All 13 pull request template headings match the workflow validation list exactly. The extracted CI step simulated in Git Bash: complete pull request body exits 0; a body missing `## Next step` exits 1. markdownlint-cli2 (markdownlint v0.41.1) on the 40 repository Markdown files: 0 issues. lychee v0.24.2 `--offline --include-fragments`: 0 errors across 109 unique links. EditorConfig content rules (LF, final newline, no trailing whitespace, no BOM; `mvnw.cmd` checkout formatting is managed by `.gitattributes`) verified against every index blob; the local `ec` binary reports the Windows CRLF working-tree checkout and is not representative of the CI checkout. `docker compose config --quiet` exit 0 (static check; daemon not required). `git diff --check` clean.
- Human Verification: Required (post-merge) — GitHub reads Issue Forms and pull request templates from the default branch, so the composer smoke check for both surfaces can only run after merge. Template contents were validated in this change; the composer check is the next step.
- CI Verification: pending until the exact PR head runs GitHub Actions; not claimed locally.
- Next step: PR -> CI/review -> merge, then run the GitHub composer smoke check for the Issue Form and the pull request template on the next Issue or pull request.

## SS-029 — Enrich Product discovery experience (#45)

- State changes: `Product` gained a validated non-blank `description`; `CreateProductRequest`, `ProductResponse`, `ProductApplicationService.createProduct`, and `ProductController` propagate it through create, list, and get. Cart-facing `CatalogProduct` and the `cart` package are unchanged. Frontend `ProductResponse`/`CreateProductRequest` carry `description` and `isProductResponse` requires exactly four keys. `Catalog.tsx` adds a required Description field, card descriptions, local product selection with a `ProductDetail` view (media, name, description, price, Add to Cart, Back to Products), focus-on-open, and preserved direct card-level Add to Cart. `Back to Products` exists only inside `ProductDetail`, and the empty Cart renders no navigation action. Cart panel stays mounted and Cart state is preserved.
- Decisions: description validation is domain-owned; description stays out of the Catalog-to-Cart contract; detail uses the already-loaded Product with local state and no router or new dependency; strict four-field response validation is intentional; a single in-detail return action and a static empty Cart are deliberate; no ADR is required.
- Agent Verification: backend `mvnw.cmd clean verify` — 37 tests, BUILD SUCCESS. Frontend `typecheck` clean, `npm.cmd test` — 3 files / 16 tests passed, `npm.cmd run build` succeeded. `git diff --check` clean. `CartApiTest` product-creation helper was updated to include a description, a necessary fixture change.
- Human Verification: passed — Product creation with name, description, and price; product cards and ProductDetail; Add to Cart from card and detail; Cart state, quantity, removal, and totals; a single `Back to Products` action inside ProductDetail; no navigation action inside the empty Cart; desktop, narrow layouts, keyboard, focus, and setup tools.
- CI Verification: pending until the exact PR head runs GitHub Actions; not claimed locally.
- Next step: PR -> CI/review -> merge, then start the next planned capability.
