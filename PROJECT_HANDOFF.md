# Project Handoff

Compact, current-state handoff notes. Each entry records only confirmed state changes, decisions, evidence, pending items, and the next step.

Update rule: this snapshot must be updated in the same change when project state, durable decisions, active work, or the next step changes. It must not be updated for routine per-Issue progress, transient branch or working-tree details, or every completed Issue; completed work stays condensed to durable outcomes, and superseded details are removed. It is a project snapshot, not a per-Issue work log or conversation archive.

## SS-030 — Formalize Issue and Project Handoff Protocol (#47)

- State changes: `docs/AI-GOVERNANCE.md` defines the canonical Issue Handoff (11 ordered fields with the controlled `Planned`/`Awaiting approval`/`In Build`/`Blocked`/`Paused`/`Completed` status), Pull Request Handoff (12 fields), and `PROJECT_HANDOFF.md` snapshot rules. `.github/ISSUE_TEMPLATE/work-item.yml` renders the Issue Handoff through one Issue Form with all 11 canonical items required; the Decisions and assumptions, Relevant files and documentation, and Pending questions or blockers fields instruct authors to write `None` when nothing applies. GitHub submits only the form's input fields, so a form-created issue body contains the canonical items in order without the form's display-only `markdown` introduction or a literal `## Issue Handoff` heading. `.github/ISSUE_TEMPLATE/config.yml` disables blank issues; neither file sets labels, assignees, or Project metadata. The Issue Handoff applies to new Issues; existing and historical Issues, including #47, are not retrofitted, as stated in `docs/AI-GOVERNANCE.md` and `CONTRIBUTING.md`. `.github/pull_request_template.md` provides the pull request handoff headings, and the existing Repository validation job fails when a required heading is missing, with `edited` added to the `pull_request` trigger so body corrections re-run it. `AGENTS.md` adds the session resume procedure, `CONTRIBUTING.md` documents contributor handoff expectations, `docs/GITHUB.md` records the Issue Forms control, and `docs/TESTING.md` describes the validation. Required status checks remain deferred.
- Decisions: one canonical protocol with pointers from AGENTS/CONTRIBUTING; one generic Issue Form with every canonical field required and `None` guidance for decision, file, and pending-question fields; the Issue Handoff applies to new Issues only, so existing and historical Issues including #47 are not retrofitted; the Issue Form is the enforced UI path while API- or agent-created issues must reproduce the canonical items manually; the canonical requirement is the ordered item set, not a literal `## Issue Handoff` heading in form-created issue bodies; the owner approved the narrow default-branch-only Human Verification exception documented in `docs/AI-GOVERNANCE.md`; no automatic labels, assignees, or Project metadata; no ADR; no product code or dependencies.
- Agent Verification: Issue Form and workflow YAML inspected (canonical field order, controlled dropdown, required fields, no automatic metadata, blank issues disabled, three jobs and `contents: read` unchanged). All 13 pull request template headings match the workflow validation list exactly. The extracted CI step simulated in Git Bash: complete pull request body exits 0; a body missing `## Next step` exits 1. Review corrections re-verified: the parsed Issue Form requires all 11 canonical items across 12 required controls, preserves order and the controlled status dropdown, and the template/validation heading match still holds; markdownlint-cli2 (markdownlint v0.41.1) on the 40 repository Markdown files: 0 issues; lychee v0.24.2 `--offline --include-fragments`: 0 errors across 109 unique links; EditorConfig content rules (LF, final newline, no trailing whitespace, no BOM; `mvnw.cmd` checkout formatting is managed by `.gitattributes`) verified against every index blob; the local `ec` binary reports the Windows CRLF working-tree checkout and is not representative of the CI checkout. `docker compose config --quiet` exit 0 (static check; daemon not required). `git diff --check` clean.
- Human Verification: pending after merge under the owner-approved default-branch-only exception documented in `docs/AI-GOVERNANCE.md` — GitHub renders Issue Forms and pull request templates from the default branch, so the composer smoke check for both surfaces cannot run before merge. Human engineering review and CI remain required before merge; the exception is narrow and change-specific. Follow-up: run the composer smoke check on the next Issue and the next pull request created after merge, and open a corrective PR or Issue immediately if it fails. Not performed; not claimed as passed.
- CI Verification: passed for the final implementation state — Repository validation (including the handoff-heading step), Backend, Frontend, CodeQL, and GitGuardian all succeeded; exact remote evidence is recorded in PR #48.
- Next step: PR -> CI/review -> merge, then run the GitHub composer smoke check for the Issue Form and the pull request template on the next Issue or pull request; if the check fails, open a corrective PR or Issue immediately.

## SS-029 — Enrich Product discovery experience (#45)

- Completed and merged via PR #46 (squash commit `c61aa2b`): a validated non-blank `description` end-to-end through the Product API and frontend, card descriptions, a local `ProductDetail` view with focus-on-open, preserved direct card-level Add to Cart, and an unchanged Cart contract. No ADR.
- Verification: backend 37 tests, frontend 16 tests, and the frontend production build passed locally; Human Verification passed, including desktop and narrow layouts, keyboard and focus behavior, and Cart state.
- CI Verification: Repository validation, Backend, Frontend, CodeQL, and GitGuardian all passed on PR head `0fe50fd` (verified remotely) before the squash merge.
